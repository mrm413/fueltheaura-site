---
layout: main.njk
title: Blog - FuelTheAura
description: Evidence-based guides, reviews, and tools for better energy.
---

<div class="page-header">
    <div class="container">
        <h1>Energy & Wellness Blog</h1>
        <p class="page-subtitle">Science-backed insights to optimize your daily energy</p>
    </div>
</div>

<section class="blog-listing">
    <div class="container">
        <div class="blog-posts">
            {% for post in collections.blogPosts %}
            <article class="blog-post-preview">
                <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
                <div class="post-meta">
                    <span class="post-date">{{ post.data.date | date }}</span>
                    {% if post.data.tags %}
                    <span class="post-category">{{ post.data.tags[0] }}</span>
                    {% endif %}
                </div>
                <p class="post-excerpt">{{ post.data.description | truncate(200) }}</p>
                <a href="{{ post.url }}" class="read-more">Continue Reading →</a>
            </article>
            {% endfor %}
            
            {% if collections.blogPosts.length == 0 %}
            <p>No blog posts yet. Check back soon!</p>
            {% endif %}
        </div>
        
        <aside class="blog-sidebar">
            <div class="sidebar-widget">
                <h3>Recent Posts</h3>
                <ul>
                    {% for post in collections.blogPosts | limit(5) %}
                    <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
                    {% endfor %}
                </ul>
            </div>
            
            <div class="sidebar-widget">
                <h3>Categories</h3>
                <ul>
                    <li><a href="/blog/">All Posts ({{ collections.blogPosts.length }})</a></li>
                    <li><a href="/blog/">Health & Wellness</a></li>
                    <li><a href="/blog/">Mental Health</a></li>
                    <li><a href="/blog/">Physical Health</a></li>
                </ul>
            </div>
        </aside>
    </div>
</section>